# Stage 1 DPI Motif Validation Report

Status: **AWAITING FROZEN RUN**

The R2 decision must be generated from the unmodified Stage 1 configuration
and recorded evidence. The reporting step selects a development-only adjacent
tolerance pair, validates that pair across every required cell, and records
either **PROCEED** or **REASSESS** without selecting a public default tau.

Generated evidence is stored under `results/generated/stage1_dpi/`; this
template intentionally contains no result before the frozen 243,000-row run.
